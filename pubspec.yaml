name: core_sim_ia
description: Flutter Clean Architecture template with Riverpod.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.7
  flutter_riverpod: ^2.6.1
  google_fonts: ^6.3.2
  riverpod_annotation: ^2.6.1

  dio: ^5.9.2
  dartz: ^0.10.1
  flutter_secure_storage: ^10.2.0

dev_dependencies:
  build_runner: ^2.5.4
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  riverpod_generator: ^2.6.5

flutter:
  uses-material-design: true
